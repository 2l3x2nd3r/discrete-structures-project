<br>

<div class="row">
  <div class="span16">
    <h3> Generar todas las particiones de un entero dado en orden lexicográfico utilizando una función recursiva.</h3>
    <br><br>
    <div class="span5">
      <div class="col-lg-8">

        <%= form_tag(:attachment1, :method => "get", id: "search-form1") do %>

          <div class="form-group">
            <%= label_tag 'n: ', nil, class: 'col-lg-2 control-label' %>
            <div class="col-lg-10">
              <%= text_field_tag :n, params[:n], placeholder: "Any valor", size: 30 %>
            </div>
          </div>

          <br><br>

          <div class="form-group">
            <div class="col-lg-10">
              <%= submit_tag "Search", :name => nil, class: 'btn btn-default' %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
    <div class="span5">
      <table class="table-bordered table-striped table-condensed table-hover">
          <% @selectables.each do |sub_selectables| %>
            <tr>
              <% sub_selectables.each do |selectable| %>
                <td>
                  <center><h3><%= selectable.to_s.gsub!('[','').gsub!(']','') %></h3></center>
                </td> 
              <% end %> 
            </tr>
          <% end %>
        </table>
      <% if @selectables != [] %>
        <%= will_paginate @selectables, renderer: BootstrapPagination::Rails %>
      <% end %>
    </div>
  </div>