<br>

<div class="row">
  <div class="span16">
    <h3> Listar todos los subconjuntos de K elementos, formados a partir de los elementos del conjunto S = {1,2,3,...,n}, que no contengan elementos consecutivos.</h3>
    <br><br><br><br>
    <div class="span5">
      <div class="col-lg-8">

        <%= form_tag(:attachment2, :method => "get", id: "search-form") do %>

          <div class="form-group">
            <%= label_tag 'S:', nil, class: 'col-lg-2 control-label' %>
            <div class="col-lg-10">
              <%= text_field_tag :array, params[:array], placeholder: "Array separated by comma", size: 30 %>
            </div>
          </div>

          <br><br>

          <div class="form-group">
            <%= label_tag 'k: ', nil, class: 'col-lg-2 control-label' %>
            <div class="col-lg-10">
              <%= text_field_tag :k, params[:k], placeholder: "Should be less to array size", size: 30 %>
            </div>
          </div>

          <br><br>

          <div class="form-group">
            <div class="col-lg-10">
              <%= submit_tag "Search", :name => nil, class: 'btn btn-default' %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
    <div class="span5">
      <table class="table-bordered table-striped table-condensed table-hover">
        <% @selectables.each do |selectable| %>
          <tr>
            <td>
              <center><h3><%= selectable.to_s.gsub!('[','').gsub!(']','') %></h3></center>
            </td> 
          </tr>
        <% end %>
      </table>
      <% if @selectables != [] %>
        <%= will_paginate @selectables, renderer: BootstrapPagination::Rails %>
      <% end %>
    </div>
  </div>